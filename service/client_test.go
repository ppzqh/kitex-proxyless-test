package service

import (
	"testing"
)

func TestProxylessClient_Benchmark(t *testing.T) {
	//svr := NewProxylessServer()
	//go svr.Run()
	//time.Sleep(time.Second)
	//
	//normalCli, err := greetservice.NewClient("1", client.WithHostPorts(":8888"))
	//test.Assert(t, err == nil)
	//Benchmark(normalCli)
}
